<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>

    <!-- Change 0.4 (canary-multiplatform): target the neutral net10.0 TFM instead of
         inheriting net10.0-windows from Directory.Build.props. -->
    <TargetFramework>net10.0</TargetFramework>

    <!-- Guardrail (change 0.4): this assembly must not depend on WinForms. Overrides the
         repo-wide UseWindowsForms=true from Directory.Build.props. -->
    <UseWindowsForms>false</UseWindowsForms>
  </PropertyGroup>
  <PropertyGroup Condition="'$(Configuration)'=='Debug'">
    <DefineConstants>$(DefineConstants);TRACE_REVISIONREADER</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="..\GitExtUtils\Validates.cs" Link="Validates.cs" />
  </ItemGroup>
  
  <ItemGroup>

    <!-- Settings -->
    <None Update="Properties\Settings.settings" Generator="SettingsSingleFileGenerator" LastGenOutput="Settings.Designer.cs" />
    <Compile Update="Properties\Settings.Designer.cs" AutoGen="True" DependentUpon="Settings.settings" />

  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="JetBrains.Annotations" />
    <PackageReference Include="System.Configuration.ConfigurationManager" />
    <PackageReference Include="System.Drawing.Common" />
    <PackageReference Include="System.IO.Abstractions" />
    <PackageReference Include="System.Reactive.Linq" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\GitExtensions.Extensibility\GitExtensions.Extensibility.csproj" />
    <ProjectReference Include="..\GitExtUtils\GitExtUtils.csproj" />
    <ProjectReference Include="..\..\plugins\GitUIPluginInterfaces\GitUIPluginInterfaces.csproj" />
  </ItemGroup>

</Project>
